# H2

spring:
  application:
    name: reservas-service
  cloud:
    config:
      uri: http://localhost:8094
      username: root
      password: s3cr3t
  h2:
    console:
      path: /h2
      enabled: true

# Datasource
  datasource:
    Url: jdbc:h2:file:~/prueba8
    driverClassName: org.h2.Driver
    platform: h2
    username: root
    password: root


